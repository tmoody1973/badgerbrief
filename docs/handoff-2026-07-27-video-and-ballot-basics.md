# Handoff — BadgerBrief, session 2026-07-27 (video + decision-support)

**Date:** 2026-07-27 · **Repo:** `/Users/tarikmoody/Documents/Projects/badgerbrief` · branch `main` at **`ebfaa23`**, synced with origin. Everything in §1 is **merged + deployed to badgerbrief.org** (prod convex `precious-axolotl-906`) unless noted.

**Next session's focus (in order):** **(1) the BadgerBrief explainer VIDEO** (Higgsfield Vox/diorama — in-flight, §3, one style decision + credits pending). **(2)** MOO-413 editorial review is now tool-assisted — Tarik's call to work the queue in `/admin`. **(3)** parked: MOO-410 GLOBAL-cap alert; retry the reverted chat ballot-basics tool.

## 0. Read first (don't re-derive)
- **Memory index:** `~/.claude/projects/-Users-tarikmoody-Documents-Projects-badgerbrief/memory/MEMORY.md`; most state in `badgerbrief-decision-support-gap.md` (MOO-409 epic + Feature A/B + MOO-410 + MOO-413 triage + /admin cluster + /ballot-basics + chat-tool gate-fail) and the SDD ledger `.superpowers/sdd/progress.md` (every task's commits).
- **Convex rules:** read `convex/_generated/ai/guidelines.md` before Convex work. **Next 16 is NOT stock** — read `node_modules/next/dist/docs/` before App Router code (AGENTS.md).
- **Process that worked:** `superpowers:brainstorming → writing-plans → subagent-driven-development` (fresh implementer per task + per-task spec/quality review + **opus whole-branch review** at the end) → `finishing-a-development-branch`. Specs `docs/superpowers/specs/`, plans `docs/superpowers/plans/`, ledger `.superpowers/sdd/progress.md`.

## 1. Shipped + deployed this session (live, don't redo)
- **Feature A — "Make sense of the guide"** (merge `d713a77`): `WhatThisMeans` inline explainers (native `<details>`, content map `src/lib/explainers.ts`) on candidate/compare/match; guided path `/start` + `GuidedRail` (renders only on `?guide=`, mounted in root layout under `<Suspense>`); "New here? Start here" home CTA. **Prod-verified** (Critical fix: `/match` `router.replace` was stripping `?guide` — now preserves all params).
- **MOO-410 — un-gate chat + guardrails** (merge `bd70b93`): Voter Help `/chat` is OPEN to guests. `chat_usage` day-counter table + hand-rolled caps in `sendMessage` (kill switch `VOTER_HELP_DISABLED` via `isKillSwitchOn`; guest global cap `GUEST_DAILY_CAP=500`; per-guest `GUEST_MSG_CAP=30`; signed-in bypass); guest id = client `localStorage bb_guest_id`. Agent bounds: `maxOutputTokens=1024` + Anthropic prompt caching + `getRaceInfo` trim. **Golden gate PASSED (golden-expectations 93%→100%)**; live-verified a guest got a cited answer.
- **Feature B — "Help improve the guide"** (merge `3a963ea`): reuses the `feedback` table/mutation/admin-queue (no new table). New `feedback.kind`s (suggest_candidate/suggest_source/data_gap/volunteer); `/contribute` hub + form; `ShareButton`; contextual "Suggest a source · Flag a gap" on candidate/race pages; footer link. **Security fix**: `safeUrl()` guard in `feedback.submit` rejects non-http(s) URL schemes (stored-XSS on the public unauth mutation). Prod-verified.
- **MOO-413 tooling** (merge `b7667d5`): `/admin → Editorial` now has a **QUEUE | BY CLUSTER** toggle. Cluster view (`src/components/admin/position-clusters.tsx`) groups pending drafts by candidate+issue, highlights the KEEP (highest confidence), one-click **Approve & publish**, per-cluster **Reject N duplicates**, NEW/LOW filters. Backend `adminQueue.positionClusters` + `publish.bulkRejectPositions` (both `requireAdmin`). **Invariant:** reject sets draft `rejected` + task `dismissed`; publish resolves the task — else it lingers in `adminQueue.list` (reads open tasks). Prod-verified. Also two **read-only review artifacts** (console + new-coverage worklist) at claude.ai/code/artifact URLs (in memory).
- **Home support box** (merge in `ce5a430`): "One person builds this guide" reader-support box under the hero (self-funded note + Ko-fi CTA + WIP→/feedback), EN/ES.
- **/ballot-basics — party & primary explainer** (MOO-414; merges `416beb5`, `c1e4ca2`, `ebfaa23`): neutral, sourced page — how the Aug 11 partisan primary works (pick one party, can't split), the party-vs-label glossary (progressive/moderate/conservative/libertarian, **balanced parallel framing**), and a **"socialist / democratic socialist / communist — what's the difference?"** section (democracy = the dividing line; DSA + Libertarian Party sourced; "labels get thrown every direction — don't vote a label"). **Two opus neutrality reviews** verified balance + factual accuracy vs Wis. Stat. `party-labels` explainer topic placed on candidate pages (under the party badge), race pages, and `/match`; `/vote` links `/ballot-basics`.

## 2. Parked / reverted (real work, not lost)
- **Chat ballot-basics tool — BUILT then REVERTED (failed golden gate).** `getBallotBasics` tool (returns vetted `src/lib/ballot-basics-content.ts` + link) + scoped rule 11, to let the chat answer party/ideology/primary questions. Deployed to prod → **golden-expectations 93%→83% (regression)**: rule 11 leaked into UNRELATED answers (who's-running, vote questions) despite tight scoping — the MOO-412 lesson again. **Reverted** (checkout main + `convex deploy` restored the certified agent). Code **preserved on branch `feat/chat-ballot-basics-tool`** (commits `f465124`+`eec20db`). To retry: make rule 11 impossible to leak into race/vote answers (or gate it harder), re-gate on prod.
- **MOO-410 GLOBAL-cap alert** — never built. Scoped: fire an `alerts` row on the message that *reaches* the guest daily cap (`globalCount === cap`, which commits — can't insert-then-throw in the same mutation). ~10 lines in `sendMessage` + a test.
- **MOO-413 editorial review itself** — approving the **24 new-coverage** drafts + rejecting the **193 duplicates** is Tarik's editorial call in `/admin` (now has the cluster tool). Queue = 300 pending → 107 distinct positions (24 new, 83 re-confirm) + 193 dup drafts.

## 3. IN-FLIGHT — BadgerBrief explainer video (Higgsfield Vox)
**Request:** a **90-second** video — what BadgerBrief is, all features, how to use it; wants screenshots; sized for **LinkedIn + YouTube (→ 16:9)**. Tarik pasted the **paper-diorama documentary** style prompt and chose "connect Higgsfield, then do the Vox/diorama version."

**Higgsfield setup state:**
- **MCP added** (`~/.claude.json`, user scope): `higgsfield → https://mcp.higgsfield.ai/mcp` (HTTP) — **status: "Needs authentication."** Tarik must run **`/mcp` → higgsfield → Authenticate** (browser OAuth), then **restart the session** for `mcp__higgsfield__*` tools to load. (He dismissed the /mcp dialog this session — likely not yet authed.)
- **`higgsfield` CLI is ALREADY authenticated** (`~/.npm-global/bin/higgsfield`, `higgsfield auth token` returns a live token). Has `gemini_omni` + `seedance_2_0` + `voices` + `workflow`/`model`/`generate`/`account`. **This is a working path WITHOUT the MCP** — the vox pipeline can run entirely via the CLI (`export PATH="$HOME/.npm-global/bin:$PATH"`).
- **Credits: 615.1** (plus plan). Budget note: gemini_omni ≈30cr/10s clip → 9 clips ≈270cr (fits); seedance_2_0 720p ≈45cr → ≈405cr (fits), 1080p ≈90cr → ≈810cr (**over budget**). So gemini_omni, or seedance 720p.
- **Companion skills installed** (`.agents/skills/`, untracked): `higgsfield-video-explainer` (the pipeline), `higgsfield-generate`, `-websites`, `-game-generation`, `-marketplace-cards`, `-product-photoshoot`, `-soul-id`. Also modified `skills-lock.json`.

**Decisions still to lock before spending (post a plan message first — skill rule):**
1. **Style:** the cinematic **diorama documentary** Tarik pasted (sepia newsprint, censor-bar faceless figures, burnt-orange accent, letterpress labels, fake-oner FPV, war-report narrator) **vs the Mixed-Media collage** (skill default). **RECOMMEND Mixed-Media (or a softened diorama):** the diorama's ominous war-report tone is off-brand for a calm, nonpartisan, trustworthy voter guide. Tarik picked "diorama" but was shown this recommendation — reconfirm.
2. **Screenshots:** the generative pipeline **cannot embed real UI screenshots** (AI clips only; no readable text). Tarik accepted the abstract path by choosing generative. Option: composite real screenshots (capturable via claude-in-chrome/playwright from live badgerbrief.org) in post — that's beyond the pure vox pipeline. If he really wants a UI/feature walkthrough with screenshots, that's a **HyperFrames** build instead (available, no Higgsfield).
3. **90s → 9 blocks × 10s.** 16:9 (pass `aspect_ratio` explicitly per block; the style key alone doesn't set framing). Documentary narrator via `voices`. Through-line object + question-hook per the skill's story engine.

**Immediate first step for the video:** decide MCP-authed-restart vs CLI path; if MCP, wait for auth+restart then verify `mcp__higgsfield__*` tools; if CLI, run via `higgsfield` CLI now. Then: research BadgerBrief's real features → 9-block script → style key → clips → voice → assemble → verify. The `vox-motion-graphics` skill (`~/.claude/skills/vox-motion-graphics/`, references `vox-prompts.md` + `diorama-doc.md`) is the playbook (written for the MCP tool names, but each maps to a CLI command).

## 4. Landmines / gotchas (this session)
- **Deploy needs BOTH** `npx convex deploy --yes` **and** `npx vercel --prod` — vercel does NOT deploy Convex. Always live-verify client-interactive pages after deploy.
- **Golden gate law** (`npm run eval:gate -- --name X --baseline sonnet-5-tuned`): any `voterHelp` agent change (tools/instructions/bounds) → deploy-to-prod → eval → **revert on fail**. Only golden-expectations gates (≥90% floor, ≤5pt drop). A new rule/tool easily **leaks into unrelated answers** and tanks the gate (happened twice now — MOO-412 rule 10, and the reverted chat ballot-basics tool). Keep new rules impossible to leak.
- **`/es` has NO races/match/candidates/contribute/ballot-basics routes** (those are EN-only) → EN-only explainers/links there are safe; the i18n key-parity test (`static-i18n.test.ts` / `chrome-i18n`) forces new dict keys into BOTH en+es dicts.
- **Higgsfield MCP** needs browser OAuth + session restart to expose tools; the **CLI is the ready fallback** (already authed).
- **Untracked cruft** to clean when convenient: `.agents/skills/` + `skills-lock.json` change (from `npx skills add`); `MEMORY/WORK/20260726-task5-guided-path-step-model/PRD.md` (stray subagent artifact); a "[automated deploy test]" row in the `/admin` feedback queue (from Feature B live-verify).
- **Linear MCP token was expired all session** — couldn't read/update issues. Re-auth, then file this session's work under MOO-409 (Feature A/B done, MOO-410 done, MOO-413 tooling done, MOO-414 = /ballot-basics done).

## 5. Suggested skills next session
- Video: `vox-motion-graphics` (or `higgsfield-video-explainer`) once Higgsfield is authed; `hyperframes` if he switches to a screenshot/UI walkthrough.
- Any chat-tool retry: `superpowers:writing-plans` + the golden-gate loop (deploy→eval→revert).
- `superpowers:verification-before-completion`; live-verify + `higgsfield account status` for credits before any paid run.
